<%= form_with model: [@post, @comment] do |form| %>
  <% @comment.errors.full_messages.each do |error| %>
    <div><%= error %></div>
  <% end %>
  
  <div>
    <%= form.label :text%>
    <%= form.text_area :text%>
  </div>

  <%= form.submit %>
<% end %>