<%= form_with model: [@user, @post, @comment] do |form| %>
  <% @post.errors.full_messages.each do |error| %>
    <div><%= error %></div>
  <% end %>
  
  <div>
    <%= form.label :text%>
    <%= form.text_area :text%>
  </div>

  <%= form.submit %>
<% end %>